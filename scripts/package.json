{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "generate-project-image-variants": "tsx ./src/generate-project-image-variants.ts",
    "generate-image-variants": "tsx ./src/generate-image-variants.ts",
    "render-pricing-pdf": "node ./src/render-pricing-pdf.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
